spring.application.name=Asclepio

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

api.security.token.secret=${JWT_SECRET:my-secreat-key}

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB

#storage-service.url=http://localhost:8081
#services.storage.url=http://localhost:8081
#

# application.properties (NÃO commitar valor real, só a referência)
security.password.pepper=${PASSWORD_PEPPER}

#
#
storage-service.url=${STORAGE_SERVICE_URL}
services.storage.url=${STORAGE_SERVICE_URL}

#https://asclepio-storage-service.onrender.com
